{% set name = "steinbock" %}
{% set version = "0.16.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/steinbock-{{ version }}.tar.gz
  sha256: 72ec65ef4baaff824b924bc2a6d55fa6eab9bf4ab4d365cbde58add55b24d08d

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0
  entry_points:
    - steinbock = steinbock._cli:steinbock_cmd_group

requirements:
  host:
    - pip
    - python >=3.8
    - setuptools-scm
  run:
    - anndata
    - click
    - click-log
    - fcswrite
    - h5py
    - imageio
    - networkx
    - numpy
    - opencv
    - pandas
    - python >=3.8
    - scikit-image
    - scipy
    - tifffile
    - xtiff

test:
  imports:
    - steinbock
  commands:
    - pip list
    - which steinbock  # [not win]
    - where steinbock  # [win]
    # - pip check  # Fails due to opencv-python-headless
  requires:
    - pip

about:
  home: https://pypi.org/project/steinbock
  summary: A toolkit for processing multiplexed tissue images
  license: MIT
  license_file: LICENSE
  dev_url: https://github.com/BodenmillerGroup/steinbock

extra:
  recipe-maintainers:
    - goanpeca
    - Milad4849
